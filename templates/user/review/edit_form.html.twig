{% extends 'base.html.twig' %}

{% block title %}{{ review.title }}{% endblock %}

{% block body %}
    {{ form_errors(form) }}
    {{ form(form) }}
{#            <a href="{{ path('review_update', { id: review.id }) }}" class="btn btn-block btn-success">Save</a>#}
            <a href="{{ path('review_delete', { id: review.id }) }}" class="btn btn-block btn-danger">Delete</a>
{% endblock %}
