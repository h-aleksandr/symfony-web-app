{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
    <div class="example-wrapper">
        {% for user in users %}
            <li>{{ user.email}} <br> {{ user.id }}</li>
        {% endfor %}
    </div>
{% endblock %}
