{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="form-control-plaintext text-center">
        <h1><b>{{ title | upper }}</b></h1>
    </div>

    <div class="margin-8"></div>

    {% for review in reviews %}
        <hr>
        <h3>{{ review.title | upper}}</h3>
        {#        <a href="{{ 'review' }}/{{ review.id }}"></a>#}
        <hr>
        <a href="{{ path('review', {id: review.id}) }}" class="btn btn-warning pull-right">More</a>
        <span><h5><b>Author's assessment: {{ review.assessment }}</b> || <b>Rating: ?</b> || <b>Like</b></h5></span>
        <span><b>Tags: </b></span>
        <hr>
        <hr>
        <br>
        <br>

        <div class="margin-8"></div>
    {% else %}
        <tr>
            <td colspan="6">There are not records yet.</td>
        </tr>
    {% endfor %}

{% endblock %}
